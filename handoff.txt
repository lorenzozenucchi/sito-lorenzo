================================================================================
ANTEPRIMA RISTORANTE — troppo spazio vuoto in alto nel riposo del pan
================================================================================

Le tre anteprime vanno bene, confermato da Lorenzo. Un solo difetto:
`immagini/anteprima-ristorante.png` ha molto spazio vuoto (buio, senza
testo) fra la barra in alto e il titolo "Il sapore vero della valle" —
guardala con il tool Read/Playwright prima di agire, per vedere esattamente
quanto. Il pan attuale (`@keyframes mock-pan`, in index.html) riposa a
`translateY(0)` sia allo 0% che al 100% del ciclo, e scende solo per un
istante al 50%: significa che la card della trattoria passa la maggior
parte dei suoi 18 secondi a mostrare proprio quello spazio vuoto in cima,
non il contenuto.

## OBIETTIVO

Ritaglia (trim) la parte superiore vuota di `immagini/anteprima-
ristorante.png`, quanto basta perché il bordo superiore dell'immagine
ritagliata sia già vicino a dove inizia il contenuto interessante (titolo o
poco prima) — non serve eliminare la barra con "Trattoria del Ponte" e
"Chiama ora" se ci sta comodamente, ma il grande vuoto sotto va tolto.
Aggiorna anche l'attributo `height` sul tag `<img>` di quella card in
index.html se cambia l'altezza reale del file (l'attributo `width` resta
uguale). Non toccare le altre due immagini né la pagina `esempi/
ristorante.html`: il problema è solo nel ritaglio usato per l'anteprima.

Verifica il risultato con Playwright: la card della trattoria, osservata a
riposo (senza aspettare il pan), deve mostrare già qualcosa di riconoscibile
— non un rettangolo prevalentemente vuoto.

Nessuna modifica a `CLAUDE.md`. Nessuna catena di revisione.

## VINCOLI

  - Non toccare `esempi/ristorante.html` né le altre due immagini
    anteprima.
  - Non toccare la logica del pan (`@keyframes mock-pan`, le durate per
    card): il problema è nell'immagine sorgente, non nell'animazione.
  - Non toccare testi, prezzi, ordine delle sezioni, icone di categoria,
    illustrazione d'apertura, grana, sipario, effetti al gesto, la barra
    "foglio X/Y" (va rimossa se non lo è già, ma è un task separato — non
    reintrodurla).
  - Non fare commit né push.
  - Dubbio bloccante: fermati e fai UNA domanda mirata a Lorenzo.
