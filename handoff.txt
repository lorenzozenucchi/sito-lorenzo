================================================================================
Annulla SOLO il punto 4 del task precedente: animazioni pan delle anteprime
================================================================================

OBIETTIVO
---------
Lorenzo ha rivisto la scelta sul punto 4 dell'handoff precedente ("Pan delle
card esempio"): non vuole che le animazioni di pan siano state toccate,
comprese le durate sincronizzate a 16s, i keyframe con soste lunghe e la
variabile CSS --pan introdotta per la card ristorante. Le vuole esattamente
come prima di quel task.

Ripristina in index.html, testuale, questo blocco (era così prima della tua
modifica al punto 4, verificato leggendo il file prima di assegnare il task
precedente):

  .mock img{
    width:100%; height:auto; display:block;
    position:absolute; top:0; left:0;
    animation:mock-pan 16s ease-in-out infinite;
  }
  .mock-ristorante img{animation-duration:18s;}
  .mock-parrucchiere img{animation-duration:13s;}
  .mock-estetico img{animation-duration:20s;}
  @keyframes mock-pan{
    0%, 100%{transform:translateY(0);}
    50%{transform:translateY(-14%);}
  }

Concretamente:
- Rimuovi ogni riferimento alla variabile CSS --pan (dichiarazione e uso),
  sia in CSS che negli attributi style eventualmente aggiunti nell'HTML.
- Riporta i keyframe di `mock-pan` alla forma sopra (via unica, niente soste
  intermedie aggiuntive).
- Riporta `animation-duration` a 18s / 13s / 20s per ristorante / parrucchiere
  / estetico rispettivamente (non più 16s uniforme).
- Non toccare nient'altro: gli altri quattro punti del task precedente
  (illustrazione nascosta sotto i 900px, testo intro "scheda Google",
  occhiello "Esempi", nota B&B senza tono scusante) restano come sono, non
  rimetterli in discussione.

Fatto quando: il blocco CSS di `.mock img`, delle tre `animation-duration` e
di `@keyframes mock-pan` corrisponde esattamente a quello sopra, e non
compare più nessuna variabile --pan nel file.

VINCOLI
-------
- Non toccare esempi/ristorante.html, esempi/parrucchiere.html,
  esempi/centro-estetico.html.
- Non toccare i file PNG in immagini/.
- Non toccare nessun'altra sezione o regola CSS oltre a quella indicata sopra.
- Se durante il ripristino trovi che il codice attuale non corrisponde a
  quanto ti aspetti da questa descrizione (es. struttura diversa da quella
  presunta), fermati e segnala la discrepanza invece di improvvisare.
